source "https://rubygems.org"

# The github-pages gem bundles the exact Jekyll version + all supported plugins.
gem "github-pages", group: :jekyll_plugins

# If you want to lock Jekyll yourself (not required when you use github-pages)
# gem "jekyll", "~> 4.3"